# Proped Rabbita

Property-based, model-based UI verification for Rabbita and MoonBit.

This repository is being bootstrapped with a small framework core that treats UI state, typed actions, invariants, shrinking, and a static state atlas as first-class concepts.
