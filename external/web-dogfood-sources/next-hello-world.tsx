// Reviewed boundary derived from the MIT-licensed Next.js hello-world example.
// Upstream remains read-only; this reduced fixture preserves the static App Router render.
export default function Page() {
  return <h1>Hello, Next.js!</h1>
}
